#!/usr/bin/env bash
# Opens flameshot's capture UI: select a region, then either drag the
# thumbnail straight into another app (like macOS' screenshot preview),
# copy it, or save/annotate it from the same window.
exec flameshot gui
