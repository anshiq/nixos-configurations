{
  "schemaVersion": 1,
  "id": "io.github.alexdont.battery-limiter",
  "name": "Battery Limiter",
  "version": "0.1.1",
  "author": "alexdont",
  "license": "MIT",
  "description": "Cap your battery's charge level to extend its lifespan: set the kernel charge limit (60/80/100 or a custom value) from the bar, see battery health and cycle count. Every change is a single explicit polkit prompt — no daemons, no udev rules",
  "kinds": [
    "service",
    "bar-widget",
    "overlay"
  ],
  "entryPoints": {
    "service": "Service.qml",
    "barWidget": "BarWidget.qml",
    "overlay": "Overlay.qml"
  },
  "barWidget": {
    "displayName": "Battery Limiter",
    "description": "Shows the active charge limit; click to open charge-limit controls and battery health.",
    "category": "Hardware",
    "allowMultiple": false,
    "defaultSection": "right",
    "schema": [
      {
        "key": "showLimit",
        "type": "boolean",
        "label": "Show limit next to the icon",
        "description": "Display the active charge limit (e.g. 80) beside the battery icon. Off shows just the icon; the limit is always visible in the card.",
        "defaultValue": true
      }
    ]
  }
}
